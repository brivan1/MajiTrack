#!/usr/bin/python3

class Zone:
    def __init__(self, zone_id, zone_name, description, latitude, longitude):
        self.zone_id = zone_id
        self.zone_name = zone_name
        self.description = description
        self.latitude = latitude
        self.longitude = longitude